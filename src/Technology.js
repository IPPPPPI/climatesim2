import produce from "immer";
import { create } from "zustand";
import "./App.css";
import moneyStore from "./Game.js";

/*
export function Technology({ setTechOpen }) {
  const money = moneyStore((state) => state.money);
  return (
    <>
      <button onClick={}>ABB</button>
      <button
        className="techButton"
        type="button"
        onClick={() => setTechOpen(false)}
      >
        Close
      </button>
    </>
  );
}
*/
